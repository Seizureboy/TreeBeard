// Code generated by Arduino IoT Cloud, DO NOT EDIT.

#include <ArduinoIoTCloud.h>
#include <Arduino_ConnectionHandler.h>

const char DEVICE_LOGIN_NAME[]  = "cb89090a-5db9-4d29-87d7-19c0cb6026e6";

const char SSID[]               = SECRET_SSID;    // Network SSID (name)
const char PASS[]               = SECRET_OPTIONAL_PASS;    // Network password (use for WPA, or use as key for WEP)
const char DEVICE_KEY[]  = SECRET_DEVICE_KEY;    // Secret device password

void onThresholdHighChange();
void onThresholdLowChange();

int moisture;
int thresholdHigh;
int thresholdLow;
bool pump;

void initProperties(){

  ArduinoCloud.setBoardId(DEVICE_LOGIN_NAME);
  ArduinoCloud.setSecretDeviceKey(DEVICE_KEY);
  ArduinoCloud.addProperty(moisture, READ, ON_CHANGE, NULL, 5);
  ArduinoCloud.addProperty(thresholdHigh, READWRITE, ON_CHANGE, onThresholdHighChange);
  ArduinoCloud.addProperty(thresholdLow, READWRITE, ON_CHANGE, onThresholdLowChange);
  ArduinoCloud.addProperty(pump, READ, ON_CHANGE, NULL);

}

WiFiConnectionHandler ArduinoIoTPreferredConnection(SSID, PASS);
